---
zip|z:
    cmd: zip <filename>.zip <files>
    description: Install one or more packages with apt.
echo|e:
    cmd: echo "<message>"
    description: Echo a message
git|git:
    status|s:
        cmd: git status
        description: Run git status command
        short|s:
          cmd: git status -s
          description: Run git status -s command
    commit|c:
        cmd: git commit -m "<commit>"
        description: Run git commit comman