---
zip|z:
    cmd: zip -r <filename>.zip <files>
    description: Zip recursively
echo|e:
    cmd: echo "<message>"
    description: Echo a message
git|git:
    status|s:
        cmd: git status
        description: Run git status command
        short|s:
          cmd: git status -s
          description: Run git status -s command
    commit|c:
        cmd: git commit -m "<commit>"
        description: Run git commit command
